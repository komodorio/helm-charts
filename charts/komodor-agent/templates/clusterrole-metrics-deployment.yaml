{{- if .Values.createRbac -}}
kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "komodorAgent.clusterRole.metricsDeployment" . }}
rules:
  # Base read permissions for metrics collection (same as daemon-metrics)
{{- if .Values.capabilities.metrics }}
  - apiGroups:
    - ""
    resources:
    - configmaps
    - namespaces
    - pods
    - nodes
    - nodes/stats
    - nodes/proxy
    verbs:
    - get
    - list
  - apiGroups:
    - extensions
    - networking.k8s.io
    resources:
    - ingresses
    verbs:
    - get
    - watch
    - list
  # apiserver metrics access
  - nonResourceURLs:
    - "/metrics"
    verbs:
    - get
{{- end }}
{{- if .Values.capabilities.metrics }}
  - apiGroups:
    - apps
    resources:
    - deployments
    - replicasets
    - statefulsets
    - daemonsets
    verbs:
    - get
  - apiGroups:
    - batch
    resources:
    - jobs
    - cronjobs
    verbs:
    - get
  - apiGroups:
    - autoscaling
    resources:
    - horizontalpodautoscalers
    verbs:
    - get
    - list
  - apiGroups:
    - ""
    resources:
    - pods
    verbs:
    - watch
{{- end }}
{{- end -}}
