{{- if hasKey .Values "namespace" }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace }}
  labels:
    {{- range $key, $val := .Values.namespaceLabels }}
    {{ $key }}: {{ $val | quote }}
    {{- end }}
{{- end }}
